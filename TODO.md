# Todo

- [x] On the writer side, we should also be able to select the model to use for
  content generation. This requires adding a model selection UI in the webview
  and handling the selected model in the extension backend when generating
  content.
- [ ] Mention which metadata you want to include for the article generation
  (e.g., title, keywords, target audience).